@model IEnumerable<DeAn.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_layoutadmin.cshtml";
}

<h2 style="text-align:center">THÔNG TIN KHÁCH HÀNG</h2>


<div style="text-align:center">
    @using (Html.BeginForm("Index1", "Product"))
    {
        <b>Tìm kiếm theo:</b>@Html.RadioButton("seachBy", "NameCus")<text>Tên khách hàng</text>
        @Html.RadioButton("seachBy", "EmailCus")<text>Email khách hàng</text>
        @Html.RadioButton("seachBy", "None", true)<text>Toàn bộ</text><br />
        @Html.TextBox("seach")<input type="submit" value="Tìm kiếm" />
    }
</div>
<table class="table">
    <tr>
        <th>
           <b>ID khách hàng</b>
        </th>
        <th>
           <b>Tên khách hàng</b>
        </th>
        <th>
            <b>SĐT</b>
        </th>
        <th>
           <b>Email</b>
        </th>
        <th>
           <b>Mật khẩu</b>
        </th>
        <th>
           
        </th>
        <th>
           
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailCus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PassCus)
            </td>
            <td>
               
            </td>
            <td>
                
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Chi tiết", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Xóa", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
